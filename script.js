// array to store the books
const myLibrary = [];

// constructor for the book object
function book(title, author, pages, status) {
    this.title = title;
    this.author = author;
    this.status = status;
}
